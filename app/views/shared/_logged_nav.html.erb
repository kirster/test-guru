<%= link_to_admin_panel(current_user) %>

<span class="navbar-text">

  <%= content_tag :span do
    concat 'Welcome, '
    concat content_tag :b, current_user.first_name
    concat ' Guru '
  end %>

  <%= content_tag :p do
    concat link_to 'Logout', destroy_user_session_path, method: :delete
  end %>

</span>