<% if resource.errors.present? %>
  <div>
    <h2> <%= pluralize(resource.errors.count, 'error') %> found: </h2>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li> <%= message %> </li>
      <% end %>
    </ul>
  </div>
<% end %>