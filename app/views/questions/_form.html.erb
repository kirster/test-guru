<%= form_with model: url_model(@test, @question), local: true do |form| %>
  <%= render 'shared/errors', resource: @question %>

  <p>
    <%= form.label :body %> <br>
    <%= form.text_field :body %> <br>
  </p>

  <p> <%= form.submit %> </p>

<% end %>